#include <stdio.h>
#include <math.h>

int main() {
double theta, ratio;

printf("Enter angle (theta) in degrees: ");
scanf("%lf", &theta);

ratio = 1 / sin(theta * M_PI / 180.0);

printf("The ratio d/lambda is: %.2f\n", ratio);

return 0;
}
